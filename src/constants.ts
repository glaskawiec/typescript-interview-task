export enum Paths {
  Login = '/login',
  Items = '/items',
  Weak = '/items/weak',
  Reused = '/items/reused',
  Old = '/items/old',
  Root = '/',
}

export enum API {
  Login = 'api/login',
  Logout = 'api/logout',
  Items = 'api/items',
  User = 'api/user',
}

export const THIRTY_DAYS_TIMESTAMP = 30 * 24 * 60 * 60 * 1000;
